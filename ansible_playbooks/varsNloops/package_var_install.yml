---
- hosts: servers
  become: yes
  vars:
    packages: 
      - mysql-server
      - nginx
      - apache2
  
  tasks:
  - name: Install packages
    apt:
      name: "{{ packages }}"
      state: present
#   when: ansible_nodename == "workernode1"

#  - name: Start & enable services
#    service:
#      name: "{{ item }}"
#      state: started
#      enabled: true
#    loop: "{{ packages }}"
